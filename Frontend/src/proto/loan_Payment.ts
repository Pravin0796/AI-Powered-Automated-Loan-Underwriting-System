// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.0
//   protoc               v4.24.4
// source: loan_Payment.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { grpc } from "@improbable-eng/grpc-web";
import { BrowserHeaders } from "browser-headers";
import { Timestamp } from "./google/protobuf/timestamp";

export const protobufPackage = "loan_payment";

/** CreateLoanPaymentRequest message for making a loan payment */
export interface CreateLoanPaymentRequest {
  loanApplicationId: number;
  amountPaid: number;
  status: string;
  paymentDate:
    | Date
    | undefined;
  /** ✅ New field */
  dueDate: Date | undefined;
}

/** CreateLoanPaymentResponse message for response after creating a loan payment */
export interface CreateLoanPaymentResponse {
  /** Unique ID of the loan payment */
  loanPaymentId: number;
  /** Status message (e.g., "Payment successful") */
  status: string;
}

/** GetLoanPaymentRequest message to request an existing loan payment */
export interface GetLoanPaymentRequest {
  /** ID of the loan payment to fetch */
  loanPaymentId: number;
}

/** GetLoanPaymentResponse message containing the loan payment details */
export interface GetLoanPaymentResponse {
  loanPaymentId: number;
  loanApplicationId: number;
  amountPaid: number;
  status: string;
  paymentDate:
    | Date
    | undefined;
  /** ✅ New field */
  dueDate: Date | undefined;
}

function createBaseCreateLoanPaymentRequest(): CreateLoanPaymentRequest {
  return { loanApplicationId: 0, amountPaid: 0, status: "", paymentDate: undefined, dueDate: undefined };
}

export const CreateLoanPaymentRequest: MessageFns<CreateLoanPaymentRequest> = {
  encode(message: CreateLoanPaymentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.loanApplicationId !== 0) {
      writer.uint32(8).uint64(message.loanApplicationId);
    }
    if (message.amountPaid !== 0) {
      writer.uint32(17).double(message.amountPaid);
    }
    if (message.status !== "") {
      writer.uint32(26).string(message.status);
    }
    if (message.paymentDate !== undefined) {
      Timestamp.encode(toTimestamp(message.paymentDate), writer.uint32(34).fork()).join();
    }
    if (message.dueDate !== undefined) {
      Timestamp.encode(toTimestamp(message.dueDate), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateLoanPaymentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateLoanPaymentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.loanApplicationId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.amountPaid = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.paymentDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.dueDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateLoanPaymentRequest {
    return {
      loanApplicationId: isSet(object.loanApplicationId) ? globalThis.Number(object.loanApplicationId) : 0,
      amountPaid: isSet(object.amountPaid) ? globalThis.Number(object.amountPaid) : 0,
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      paymentDate: isSet(object.paymentDate) ? fromJsonTimestamp(object.paymentDate) : undefined,
      dueDate: isSet(object.dueDate) ? fromJsonTimestamp(object.dueDate) : undefined,
    };
  },

  toJSON(message: CreateLoanPaymentRequest): unknown {
    const obj: any = {};
    if (message.loanApplicationId !== 0) {
      obj.loanApplicationId = Math.round(message.loanApplicationId);
    }
    if (message.amountPaid !== 0) {
      obj.amountPaid = message.amountPaid;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.paymentDate !== undefined) {
      obj.paymentDate = message.paymentDate.toISOString();
    }
    if (message.dueDate !== undefined) {
      obj.dueDate = message.dueDate.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateLoanPaymentRequest>, I>>(base?: I): CreateLoanPaymentRequest {
    return CreateLoanPaymentRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateLoanPaymentRequest>, I>>(object: I): CreateLoanPaymentRequest {
    const message = createBaseCreateLoanPaymentRequest();
    message.loanApplicationId = object.loanApplicationId ?? 0;
    message.amountPaid = object.amountPaid ?? 0;
    message.status = object.status ?? "";
    message.paymentDate = object.paymentDate ?? undefined;
    message.dueDate = object.dueDate ?? undefined;
    return message;
  },
};

function createBaseCreateLoanPaymentResponse(): CreateLoanPaymentResponse {
  return { loanPaymentId: 0, status: "" };
}

export const CreateLoanPaymentResponse: MessageFns<CreateLoanPaymentResponse> = {
  encode(message: CreateLoanPaymentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.loanPaymentId !== 0) {
      writer.uint32(8).uint64(message.loanPaymentId);
    }
    if (message.status !== "") {
      writer.uint32(18).string(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateLoanPaymentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateLoanPaymentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.loanPaymentId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.status = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateLoanPaymentResponse {
    return {
      loanPaymentId: isSet(object.loanPaymentId) ? globalThis.Number(object.loanPaymentId) : 0,
      status: isSet(object.status) ? globalThis.String(object.status) : "",
    };
  },

  toJSON(message: CreateLoanPaymentResponse): unknown {
    const obj: any = {};
    if (message.loanPaymentId !== 0) {
      obj.loanPaymentId = Math.round(message.loanPaymentId);
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateLoanPaymentResponse>, I>>(base?: I): CreateLoanPaymentResponse {
    return CreateLoanPaymentResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateLoanPaymentResponse>, I>>(object: I): CreateLoanPaymentResponse {
    const message = createBaseCreateLoanPaymentResponse();
    message.loanPaymentId = object.loanPaymentId ?? 0;
    message.status = object.status ?? "";
    return message;
  },
};

function createBaseGetLoanPaymentRequest(): GetLoanPaymentRequest {
  return { loanPaymentId: 0 };
}

export const GetLoanPaymentRequest: MessageFns<GetLoanPaymentRequest> = {
  encode(message: GetLoanPaymentRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.loanPaymentId !== 0) {
      writer.uint32(8).uint64(message.loanPaymentId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLoanPaymentRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLoanPaymentRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.loanPaymentId = longToNumber(reader.uint64());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetLoanPaymentRequest {
    return { loanPaymentId: isSet(object.loanPaymentId) ? globalThis.Number(object.loanPaymentId) : 0 };
  },

  toJSON(message: GetLoanPaymentRequest): unknown {
    const obj: any = {};
    if (message.loanPaymentId !== 0) {
      obj.loanPaymentId = Math.round(message.loanPaymentId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetLoanPaymentRequest>, I>>(base?: I): GetLoanPaymentRequest {
    return GetLoanPaymentRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetLoanPaymentRequest>, I>>(object: I): GetLoanPaymentRequest {
    const message = createBaseGetLoanPaymentRequest();
    message.loanPaymentId = object.loanPaymentId ?? 0;
    return message;
  },
};

function createBaseGetLoanPaymentResponse(): GetLoanPaymentResponse {
  return {
    loanPaymentId: 0,
    loanApplicationId: 0,
    amountPaid: 0,
    status: "",
    paymentDate: undefined,
    dueDate: undefined,
  };
}

export const GetLoanPaymentResponse: MessageFns<GetLoanPaymentResponse> = {
  encode(message: GetLoanPaymentResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.loanPaymentId !== 0) {
      writer.uint32(8).uint64(message.loanPaymentId);
    }
    if (message.loanApplicationId !== 0) {
      writer.uint32(16).uint64(message.loanApplicationId);
    }
    if (message.amountPaid !== 0) {
      writer.uint32(25).double(message.amountPaid);
    }
    if (message.status !== "") {
      writer.uint32(34).string(message.status);
    }
    if (message.paymentDate !== undefined) {
      Timestamp.encode(toTimestamp(message.paymentDate), writer.uint32(42).fork()).join();
    }
    if (message.dueDate !== undefined) {
      Timestamp.encode(toTimestamp(message.dueDate), writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetLoanPaymentResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetLoanPaymentResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.loanPaymentId = longToNumber(reader.uint64());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.loanApplicationId = longToNumber(reader.uint64());
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.amountPaid = reader.double();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.status = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.paymentDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.dueDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetLoanPaymentResponse {
    return {
      loanPaymentId: isSet(object.loanPaymentId) ? globalThis.Number(object.loanPaymentId) : 0,
      loanApplicationId: isSet(object.loanApplicationId) ? globalThis.Number(object.loanApplicationId) : 0,
      amountPaid: isSet(object.amountPaid) ? globalThis.Number(object.amountPaid) : 0,
      status: isSet(object.status) ? globalThis.String(object.status) : "",
      paymentDate: isSet(object.paymentDate) ? fromJsonTimestamp(object.paymentDate) : undefined,
      dueDate: isSet(object.dueDate) ? fromJsonTimestamp(object.dueDate) : undefined,
    };
  },

  toJSON(message: GetLoanPaymentResponse): unknown {
    const obj: any = {};
    if (message.loanPaymentId !== 0) {
      obj.loanPaymentId = Math.round(message.loanPaymentId);
    }
    if (message.loanApplicationId !== 0) {
      obj.loanApplicationId = Math.round(message.loanApplicationId);
    }
    if (message.amountPaid !== 0) {
      obj.amountPaid = message.amountPaid;
    }
    if (message.status !== "") {
      obj.status = message.status;
    }
    if (message.paymentDate !== undefined) {
      obj.paymentDate = message.paymentDate.toISOString();
    }
    if (message.dueDate !== undefined) {
      obj.dueDate = message.dueDate.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetLoanPaymentResponse>, I>>(base?: I): GetLoanPaymentResponse {
    return GetLoanPaymentResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetLoanPaymentResponse>, I>>(object: I): GetLoanPaymentResponse {
    const message = createBaseGetLoanPaymentResponse();
    message.loanPaymentId = object.loanPaymentId ?? 0;
    message.loanApplicationId = object.loanApplicationId ?? 0;
    message.amountPaid = object.amountPaid ?? 0;
    message.status = object.status ?? "";
    message.paymentDate = object.paymentDate ?? undefined;
    message.dueDate = object.dueDate ?? undefined;
    return message;
  },
};

/** LoanPayment service for handling loan repayment transactions */
export interface LoanPaymentService {
  CreateLoanPayment(
    request: DeepPartial<CreateLoanPaymentRequest>,
    metadata?: grpc.Metadata,
  ): Promise<CreateLoanPaymentResponse>;
  GetLoanPayment(
    request: DeepPartial<GetLoanPaymentRequest>,
    metadata?: grpc.Metadata,
  ): Promise<GetLoanPaymentResponse>;
}

export class LoanPaymentServiceClientImpl implements LoanPaymentService {
  private readonly rpc: Rpc;

  constructor(rpc: Rpc) {
    this.rpc = rpc;
    this.CreateLoanPayment = this.CreateLoanPayment.bind(this);
    this.GetLoanPayment = this.GetLoanPayment.bind(this);
  }

  CreateLoanPayment(
    request: DeepPartial<CreateLoanPaymentRequest>,
    metadata?: grpc.Metadata,
  ): Promise<CreateLoanPaymentResponse> {
    return this.rpc.unary(
      LoanPaymentServiceCreateLoanPaymentDesc,
      CreateLoanPaymentRequest.fromPartial(request),
      metadata,
    );
  }

  GetLoanPayment(
    request: DeepPartial<GetLoanPaymentRequest>,
    metadata?: grpc.Metadata,
  ): Promise<GetLoanPaymentResponse> {
    return this.rpc.unary(LoanPaymentServiceGetLoanPaymentDesc, GetLoanPaymentRequest.fromPartial(request), metadata);
  }
}

export const LoanPaymentServiceDesc = { serviceName: "loan_payment.LoanPaymentService" };

export const LoanPaymentServiceCreateLoanPaymentDesc: UnaryMethodDefinitionish = {
  methodName: "CreateLoanPayment",
  service: LoanPaymentServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return CreateLoanPaymentRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = CreateLoanPaymentResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

export const LoanPaymentServiceGetLoanPaymentDesc: UnaryMethodDefinitionish = {
  methodName: "GetLoanPayment",
  service: LoanPaymentServiceDesc,
  requestStream: false,
  responseStream: false,
  requestType: {
    serializeBinary() {
      return GetLoanPaymentRequest.encode(this).finish();
    },
  } as any,
  responseType: {
    deserializeBinary(data: Uint8Array) {
      const value = GetLoanPaymentResponse.decode(data);
      return {
        ...value,
        toObject() {
          return value;
        },
      };
    },
  } as any,
};

interface UnaryMethodDefinitionishR extends grpc.UnaryMethodDefinition<any, any> {
  requestStream: any;
  responseStream: any;
}

type UnaryMethodDefinitionish = UnaryMethodDefinitionishR;

interface Rpc {
  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any>;
}

export class GrpcWebImpl {
  private host: string;
  private options: {
    transport?: grpc.TransportFactory;

    debug?: boolean;
    metadata?: grpc.Metadata;
    upStreamRetryCodes?: number[];
  };

  constructor(
    host: string,
    options: {
      transport?: grpc.TransportFactory;

      debug?: boolean;
      metadata?: grpc.Metadata;
      upStreamRetryCodes?: number[];
    },
  ) {
    this.host = host;
    this.options = options;
  }

  unary<T extends UnaryMethodDefinitionish>(
    methodDesc: T,
    _request: any,
    metadata: grpc.Metadata | undefined,
  ): Promise<any> {
    const request = { ..._request, ...methodDesc.requestType };
    const maybeCombinedMetadata = metadata && this.options.metadata
      ? new BrowserHeaders({ ...this.options?.metadata.headersMap, ...metadata?.headersMap })
      : metadata ?? this.options.metadata;
    return new Promise((resolve, reject) => {
      grpc.unary(methodDesc, {
        request,
        host: this.host,
        metadata: maybeCombinedMetadata ?? {},
        ...(this.options.transport !== undefined ? { transport: this.options.transport } : {}),
        debug: this.options.debug ?? false,
        onEnd: function (response) {
          if (response.status === grpc.Code.OK) {
            resolve(response.message!.toObject());
          } else {
            const err = new GrpcWebError(response.statusMessage, response.status, response.trailers);
            reject(err);
          }
        },
      });
    });
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export class GrpcWebError extends globalThis.Error {
  constructor(message: string, public code: grpc.Code, public metadata: grpc.Metadata) {
    super(message);
  }
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
